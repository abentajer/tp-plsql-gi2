CREATE Filiere(idfil INTEGER PRIMARY KEY, nomfil VARCHAR(10));

CREATE Electeurs(idelec INTEGER PRIMARY KEY, nom VARCHAR(10), pnom VARCHAR(10), ddn DATE, nvot INTEGER, nvoix INTEGER, cand INTEGER, idfil INTEGER , FOREIGN KEY (Filiere) );
